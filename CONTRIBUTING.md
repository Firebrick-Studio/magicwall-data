# Contributing Wallpapers

1. Place the new wallpaper image (`.jpg` or `.png`) into the appropriate category folder under `wallpapers/`.
2. Create a corresponding `.json` metadata file with the same name.
3. Run `dart run scripts/generate_index.dart` to update the global `index.json` file.
4. Commit and push your changes.
