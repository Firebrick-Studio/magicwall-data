# Magicwall Data

This repository contains the wallpaper catalog and metadata for the Magicwall app.

## Structure

Wallpapers are categorized into folders within the `wallpapers/` directory. Each wallpaper has a corresponding `.json` metadata file.

To fetch all wallpapers, applications should download the `index.json` file at the root of the repository, which is automatically generated and contains all metadata.
